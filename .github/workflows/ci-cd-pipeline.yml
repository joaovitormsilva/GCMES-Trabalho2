name: CI/CD Pipeline

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '18'

    - name: Install dependencies for frontend
      working-directory: guaradata-frontend-main
      run: npm install

    - name: Build frontend
      working-directory: guaradata-frontend-main
      run: npm run build


    - name: Run frontend in development mode
      working-directory: guaradata-frontend-main
      run: npm run dev &
      env:
        CI: true

  #  - name: Run frontend tests
    #  working-directory: guaradata-frontend-main
  #    run: npm test

    #- name: Install dependencies for backend
    #  working-directory: guaradata-backend-main
    #  run: npm install


    

   # - name: Run backend tests
   #   working-directory: guaradata-backend-main
 #     run: npm test

    - name: SonarCloud Scan
      uses: sonarsource/sonarcloud-github-action@v1.3
      with:
        projectKey: joaovitormsilva_GCMES-Trabalho2
        organization: joaovitormsilva
        token: ${{ secrets.SONAR_TOKEN }}
        options: >
          -Dsonar.sources=guaradata-backend-main,guaradata-frontend-main
          -Dsonar.tests=guaradata-backend-main/tests,guaradata-frontend-main/tests
          -Dsonar.inclusions=**/*.js
